#include<bits/stdc++.h>
using namespace std ; 
int nthFiboTerm(int);
int nthFiboTerm(int n)
{
    if(n==0)
        return 1 ;
    else if(n==-1)
        return -1 ;
    int n3 ;    
    n3 = nthFiboTerm(n-1) + nthFiboTerm(n-2);
    return n3 ;
}
