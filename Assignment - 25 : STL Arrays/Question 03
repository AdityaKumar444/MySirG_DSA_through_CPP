#include<bits/stdc++.h>
using namespace std ; 
int main () 
{
    array<int,10> arr ;
    int max = INT_MIN ;
    for(int i=0 ; i<10 ; i++)
    {
        cin >> arr[i] ;
        if(max < arr[i])
            max = arr[i] ;
    }
    cout << "greatest element is " << max << endl ;
    return 0 ; 
}
